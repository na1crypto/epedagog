{
  "name": "epedagog",
  "version": "1.0.0",
  "description": "E-PEDAGOG - Ta'lim muassasalari uchun elektron boshqaruv tizimi",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "install:server": "cd server && npm install",
    "start": "node server/index.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
